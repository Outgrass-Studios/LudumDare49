using Player;

namespace Entity
{
    public class BuildermanController : EntityController
    {
        public override void OnPlayerIgnore()
        {
            //Get player camera transform and rotation
            //Instantiate a brick and apply force
            //PlayerReference.Singleton.cam.transform.
        }
    }
}